name: Build
on:
  push:
    branches: [ main ]
  pull_request:
jobs:
  test:
    name: Run build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '16.x'
          cache: yarn
      - run: yarn install
      - run: yarn build